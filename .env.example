# development | test | production
NODE_ENV=

# -----------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------

# Dev/test (local): host is 'localhost'
# Docker (dev/prod): host must be 'postgres' (the Docker service name)
# External DB (Neon, Supabase, RDS, etc.): paste your provider's connection string
#   e.g. postgresql://user:pass@your-host.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://myusername:mypassword@localhost:5432/mydatabase
DB_USERNAME=myusername
DB_PASSWORD=mypassword
DB_NAME=mydatabase
DB_HOST=localhost
DB_HOST_PORT=5432

# -----------------------------------------------------------------------
# App
# -----------------------------------------------------------------------

# Long random string — generate with: openssl rand -hex 64
SECRET=mysecret

OPENING_HOUR=12
OPENING_MINUTE=30
CLOSING_HOUR=17
CLOSING_MINUTE=00
EXPIRY=800000

# -----------------------------------------------------------------------
# Email (SMTP)
# -----------------------------------------------------------------------
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASSWORD=

PERSONAL_EMAIL=

# -----------------------------------------------------------------------
# Public URL and domain
# -----------------------------------------------------------------------

# Your API's public-facing URL (used in email templates, etc.)
BASE_URL=http://localhost:8000

# Domain Caddy serves in production (must match BASE_URL hostname, no https://)
# e.g. api.yourdomain.com
DOMAIN=

# -----------------------------------------------------------------------
# Timezone
# -----------------------------------------------------------------------
TZ=America/Toronto
PGTZ=America/Toronto
